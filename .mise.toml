# mise configuration for Opportunity.ai
# mise is a polyglot tool version manager (https://mise.jdx.dev)
# It automatically manages tool versions when you enter this project directory

[tools]
# Node.js version (LTS)
node = "20.18.0"
# pnpm package manager
pnpm = "9.15.0"
# Supabase CLI (optional, can also install via npm)
# supabase = "1.234.0"

[env]
# mise can also manage environment variables per project
# Add any project-specific env vars here if needed
